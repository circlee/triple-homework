spring:
  profiles:
    include:
      - core

logging:
  level:
    guide:
      triple: debug
    org:
      springframework:
        transaction: DEBUG
        orm:
          jpa: DEBUG
      hibernate:
        type:
          descriptor:
            sql: trace
